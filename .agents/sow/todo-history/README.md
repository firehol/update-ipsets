## Historical TODOs

This directory preserves older task trackers and design-history notes that no
longer control current execution.

Current work control lives in:

- `../current/` for active SOWs
- `../done/` for completed SOWs

Use the files here as historical implementation context only. The authoritative
product and behavior contracts live under `../specs/`.
